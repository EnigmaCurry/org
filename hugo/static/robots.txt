# Crawl directives for book.rymcg.tech
#
# The whole site is otherwise open to indexing. The /notes book is unlisted
# (nothing on the site links to it); ask well-behaved crawlers not to index it
# or anything beneath it.
#
# Caveat: robots.txt is a public, advisory hint. This file itself names /notes
# to anyone who reads it, and misbehaving bots ignore it entirely. It only stops
# compliant search engines from indexing the path — it is not access control.
User-agent: *
Disallow: /notes
